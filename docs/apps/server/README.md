# IronProf Teacher app

## Environment

The environnement variables can be defined in the `.env.local` file in the `apps/server` directory.

```bash
ELECTRON=false (default) | true # Whether to start the electron wrapper or not.
```
