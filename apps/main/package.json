{
    "name": "main",
    "version": "1.0.0",
    "description": "Main server for the app",
    "private": true,
    "main": "dist/main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "tsc-watch --silent --onSuccess \"node ./dist/main.js\" --onFailure \"echo 'Build failed'\""
    },
    "devDependencies": {
        "@types/express": "^4.17.14",
        "@types/node": "^18.8.5",
        "tsc-watch": "^5.0.3",
        "typescript": "^4.8.4"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}
