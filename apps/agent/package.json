{
    "name": "agent",
    "version": "1.0.0",
    "description": "Main server for the app",
    "private": true,
    "main": "dist/main.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "dev": "cross-env NODE_ENV=development tsc-watch --onSuccess \"electron .\" --onFailure \"echo 'Build failed'\""
    },
    "devDependencies": {
        "@types/node": "^18.8.5",
        "cross-env": "^7.0.3",
        "electron": "^21.1.1",
        "electron-serve": "^1.1.0",
        "tsc-watch": "^5.0.3",
        "typescript": "^4.8.4"
    },
    "dependencies": {
        "leapjs": "^1.1.1"
    }
}
